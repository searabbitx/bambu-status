services:
  app:
    build: .
    ports:
      - "5000:5000" 
      - "80:80" 
    volumes:
      - type: bind
        source: flask
        target: /usr/src/app/
      - type: bind
        source: frontend
        target: /usr/src/frontend/
    networks:
      - net
    secrets:
      - api_key

secrets:
  api_key:
    file: api_key.txt
  
networks:
  net:
